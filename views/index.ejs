<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>homepage</title>
  </head>
  <body>
    <main>
      <section class="welcome">
        <h1>Tervetuloa</h1>
      </section>

      <form action="/maa" method="post">
        <select name="maanNimi" id="maat">
          <% for(let i = 0; i < maat.length; i++) { %>
          <option value="<%= maat[i] %>"><%= maat[i] %></option>

          <% } %>
        </select>

        <button type="submit">Hae tiedot</button>
      </form>
      <% if (!saapuminen) { %>
      <div class="landing-container">
        <h1>
          Hei! valitse maa taulukosta niin saat kyseisen maan tiedot näkyviin
        </h1>
      </div>
      <% } else { %>
      <div class="div">
        <h1>maa : <%= maa %></h1>
        <h1>pääkaupunki : <%= paakaupunki %></h1>
        <h1>asukasluku : <%= asukasluku %></h1>
        <h1>valuutta : <%= valuutta %></h1>
        <h1><img src="<%= flagUrl %>" alt="" /></h1>
      </div>
      <% } %>
    </main>
  </body>
</html>
